az aks get-credentials --resource-group aks-resource-group --name aks-cluster

kubectl config view --raw

az aks browse --resource-group Dev-RG --name Dev-AKS-Cluster

az aks get-credentials --resource-group Dev-RG --name Dev-AKS-Cluster
